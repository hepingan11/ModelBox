！！！后端文件在Java分支！！！

前端使用vue框架并引入了uniapp，可直接打包成h5,app,小程序等多种文件；

下载后请先运行:

```bash
npm install
```

运行到web(h5):

```bash
npm run dev:h5
```

运行到微信小程序：

```bash
npm run dev:mp-weixin	
```

运行到app模拟器推荐使用HbuilderX运行

打包h5：

```bash
npm run build:h5	
```

输出文件目录：dist/build/h5

