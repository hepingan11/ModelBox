package com.cn.bdth.constants;

public interface ProjectStatusConstant {

    //pending(待审核)，
    String PENDING = "pending";

    // online(通过且已上线)
    String ONLINE = "online";

    // reject(未通过)
    String REJECT = "reject";

    // not-online(通过但未上线),
    String NOT_ONLINE = "not-online";
}


